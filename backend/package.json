{
  "name": "steam-coplay-backend",
  "version": "1.0.0",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "jest tests/unit",
    "test:unit": "jest tests/unit",
    "test:integration": "jest tests/integration --testTimeout=30000",
    "test:all": "jest --testTimeout=30000"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.0",
    "express": "^4.18.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
